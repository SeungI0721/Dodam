// 도담도담 프로젝트 전체에 적용되는 Gradle 빌드 설정 파일이다.
plugins {
    id 'com.android.application' version '8.6.1' apply false
    id 'com.google.gms.google-services' version '4.5.0' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
